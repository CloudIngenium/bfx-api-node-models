node_modules/
dist/
*.tsbuildinfo
coverage/
.nyc_output/
*.tgz

# pnpm
.pnpm-store/
.pnpm-debug.log*
pnpm-store/

# Per-session git worktrees (autonomy-contract.md doctrine, scratch space only).
.worktrees/

# OS + git scratch (fleet baseline)
.DS_Store
Thumbs.db

# pnpm sync writes KH rule copies here (KH is source of truth); never commit
.claude/rules/

# husky runtime dir — generated by `husky` on install; new-worktree.sh
# symlinks it to the main checkout with an ABSOLUTE path, which breaks CI
# if committed (ENOENT on `mkdir .husky/_`).
.husky/_
