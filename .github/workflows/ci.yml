name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      matrix:
        node-version: [24]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: CloudIngenium/astro-shared/.github/actions/setup-pnpm@04d7d5f30945883f8b5224eab424d53484eef4f2 # central pnpm bootstrap
        with:
          node-version: ${{ matrix.node-version }}
      - run: pnpm install --frozen-lockfile
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: pnpm run build
      - run: pnpm run lint
      - run: pnpm test

  build-and-test:
    name: build-and-test
    needs: [test]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Verify test matrix succeeded
        run: |
          if [[ "${{ needs.test.result }}" != "success" ]]; then
            echo "test: ${{ needs.test.result }}"
            exit 1
          fi
          echo "All required checks passed"
